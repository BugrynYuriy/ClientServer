простий чат .... проект в стадії розробки ...

реалізовано :

1. Сервер : приєднує користувачів , приймає повідомлення та розсилає їх всім користувачам
2. Кліент : приєднується до сервера , відсилає та приймає повідомлення від сервера

simple chat .... project under construction ...

implemented:

A. Server: connec  users , receives messages and sends them to all users
2. Client: connected to the server, sends and receives messages from the server